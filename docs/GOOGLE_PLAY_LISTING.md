# Google Play — Prepared Listing Content & Answers

Ready-to-paste content for the [GOOGLE_PLAY.md](GOOGLE_PLAY.md) checklist.
Decisions locked: **Personal developer account**, **Free app**, **privacy policy
hosted on GitHub Pages**. Privacy policy draft lives in
[ANDROID_PRIVACY.md](ANDROID_PRIVACY.md).

> Edit the bracketed `[…]` spots and trim Chinese wording to taste before
> pasting into Play Console. Character limits are noted; current drafts are
> within them.

> **Consistency with the original 2010s listing.** LIME IME was published on
> Google Play under `net.toload.main.hd` (500,000+ installs, last updated ~2018)
> before it was removed. The historic listing — verified from Wayback captures
> (2017 & 2019) — used:
> - **Title:** `萊姆中文輸入法 - LIME IME`
> - **Developer:** `Android.toload.net`
> - **Category:** Tools
> - **Tagline:** 「一套自由開源的輸入法程式」 (a free & open-source input method)
> - **Input methods:** 自建、注音、倉頡、快倉、倉頡五、四碼倉頡、速成、大易、輕鬆、行列、行列10
>   (注音 layouts: 標準大千 / 倚天26 / 倚天41 / 許氏)
> - **Legacy links:** www.limeime.org · fb.com/limeime (no longer used)
>
> ⚠️ The legacy site **www.limeime.org is dead** (connection refused). The new
> listing uses the **GitHub Pages site** <https://lime-ime.github.io/limeime/>
> and **GitHub Issues** for contact — no public email. The drafts below keep the
> historic **title, category, and method names** for continuity (the old
> `Android.toload.net` developer account is **not** available — see Section F), but
> update links and add the new-generation features (English completion, emoji, voice).

---

## A. Store listing text (Section 5)

### App title (≤ 30 chars)

- **en-US:** `LIME IME 6 — Chinese Keyboard`  (29)
- **zh-TW:** `萊姆中文輸入法 - LIME IME 6`  (20)

> Uses **`6`** as the version marker (matches `versionName 6.x`), not `2026` — a
> year dates the app quickly and Google may flag year-stuffed titles. The base
> name `萊姆中文輸入法 - LIME IME` matches the historic Play title for continuity;
> the trailing `6` distinguishes this generation. The in-app launcher label
> (`app_name`) is set to `萊姆輸入法6` to match. Play title max is 30 characters.

### Short description (≤ 80 chars)

- **en-US:**
  `Free open-source Chinese keyboard: Zhuyin, Cangjie, Array, Dayi & more.`  (70)
- **zh-TW:**
  `自由開源中文輸入法：注音、倉頡、行列、大易、速成、輕鬆等多種輸入法與語音輸入。`  (37)

### Full description (≤ 4000 chars)

**en-US:**

```
LIME IME (萊姆中文輸入法) is a free and open-source Chinese input method,
rebuilt for modern Android. First published on Google Play with 500,000+
installs, LIME IME 6 is a ground-up rewrite for the latest Android versions.

CHINESE INPUT METHODS
Multiple input methods: Custom (自建), Zhuyin/Bopomofo (注音), Cangjie (倉頡),
Quick Cangjie (快倉), Cangjie 5 (倉頡五), 4-code Cangjie (四碼倉頡),
Speed/Simplified (速成), Dayi (大易), Boshiamy/Relax (輕鬆), Array (行列) and
Array 10 (行列10).
• Zhuyin keyboard layouts: Standard (大千), ETen 26-key, ETen 41-key, and Hsu.
• Mixed Chinese–English typing without switching keyboards.
• Simplified/Traditional conversion and root reverse-lookup.

MORE
• English typing with smart word completion.
• Emoji input.
• Optional in-keyboard voice typing (uses your device's speech service).
• Downloadable input-method tables — add the layouts you want, or build and
  share your own.
• Custom user phrases and on-device learning that improves candidate order as
  you type.
• Backup & restore your settings and dictionaries to a file you choose.
• Physical/Bluetooth keyboard support, tablet and landscape layouts.

PRIVACY FIRST
Your typing stays on your device. LIME has no server that collects your
keystrokes, learned words, or personal dictionary. The microphone is used only
for optional voice typing, handled by your device's system speech service.

OPEN SOURCE
LIME IME is free software under GPL v3. Source code:
https://github.com/lime-ime/limeime

With thanks to Dayi inventor Mr. Wang Tsan-chieh for the Dayi UNICODE licence.
Works on Android 5.0+ (API 21) and is optimised for the latest Android release.
```

**zh-TW:**

```
萊姆中文輸入法 - LIME IME，是一套自由開源的輸入法程式，為現代 Android 重新打造。
萊姆曾於 Google Play 上架並累積 50 萬次以上安裝，全新第 6 代重新回歸並支援最新 Android。

中文輸入法
支援多種輸入法：自建、注音、倉頡、快倉、倉頡五、四碼倉頡、速成、大易、輕鬆、行列、行列10。
• 注音鍵盤排列：標準注音(大千)、倚天26鍵、倚天41鍵及許氏注音。
• 中英文混打，不用切換鍵盤。
• 簡／繁字碼轉換與輸入法字根反查。

更多功能
• 英文輸入與智慧字詞補全。
• Emoji 輸入。
• 選用的鍵盤內語音輸入（使用裝置的語音服務）。
• 可下載輸入法碼表 — 自由新增想要的鍵盤配置，亦可自建並分享給朋友。
• 自訂使用者詞彙，並具備本機學習，依您的輸入習慣調整候選字排序。
• 設定與詞庫可備份／還原至您選擇的檔案。
• 支援實體／藍牙鍵盤，以及平板與橫向鍵盤配置。

隱私優先
您輸入的文字保留在裝置上。萊姆沒有任何伺服器會蒐集您的按鍵、學習詞彙或個人詞庫。
麥克風僅用於選用的語音輸入，並由裝置的系統語音服務處理。

開放原始碼
萊姆為 GPL v3 自由軟體，原始碼：
https://github.com/lime-ime/limeime

感謝大易輸入法發明者王贊傑先生，萊姆中文輸入法正式獲得大易 UNICODE 版使用授權。
支援 Android 5.0（API 21）以上，並針對最新 Android 版本最佳化。
```

### App category & contact (Section 5)

- **Category:** Tools  ← matches the historic listing. (alt: Productivity)
- **Developer (store) name:** the historic `Android.toload.net` account is **not
  available** (see Section F). Use this team's account — suggested display name
  `The LimeIME Open Source Project`.
- **Tags:** keyboard, input method, Traditional Chinese
- **Contact email:** Play Console **requires** a contact email on the listing —
  supply a dedicated alias (e.g. `[your-lime-alias@…]`), **not** a personal
  address. No email is shown in public-facing copy; users are pointed to GitHub
  Issues instead. *(Legacy `support@limeime.org` / `www.limeime.org` are dead — do not reuse.)*
- **Website:** https://lime-ime.github.io/limeime/  (GitHub Pages; repo: https://github.com/lime-ime/limeime)
- **Phone:** optional — leave blank for a personal account unless you want it shown.

### Release notes / "What's new" for 6.1.17 (≤ 500 chars)

- **en-US:**
  ```
  • Fixed English word completion: short words like "salt" no longer disappear,
    and suggestions are no longer forced into alphabetical order (#103).
  • English candidates are shown without a forced default highlight.
  • Fresh installs now activate the enabled input method correctly instead of
    falling back to English.
  • Added a bundled scored English dictionary for better suggestions.
  ```
- **zh-TW:**
  ```
  • 修正英文字詞補全：salt 等短字不再消失，候選不再被強制依字母排序（#103）。
  • 英文候選不再被預設反白。
  • 全新安裝現在會正確啟用已選輸入法，不再退回英文。
  • 新增內建評分英文字典，提供更佳建議。
  ```

---

## B. Data Safety form answers (Section 8)

Fill the Play Console **Data Safety** wizard with these answers. The honest
position for LimeIME is **"No data collected, no data shared."**

### Top-level

| Question | Answer |
| --- | --- |
| Does your app collect or share any of the required user data types? | **No** |
| Is all collected data encrypted in transit? | N/A (nothing collected/transmitted) |
| Do you provide a way for users to request data deletion? | Data is on-device; users delete via Clear data / Uninstall |

> Rationale: LimeIME stores typing/learning data **only on the device** and never
> transmits it. On-device-only storage is **not** "collection" under Play's Data
> Safety definitions. The network permission is used solely to download
> input-method tables the user requests — that is not collection of user data.

### If Play flags the microphone or you prefer to over-declare

If the review asks about `RECORD_AUDIO`, declare:

| Field | Answer |
| --- | --- |
| Data type | Audio → Voice or sound recordings |
| Collected? | **No** — audio is processed by the system speech service, not stored or sent by LimeIME |
| Shared? | No |
| Purpose | App functionality (voice typing) |

### Permissions declaration (if prompted)

| Permission | Declared use |
| --- | --- |
| `RECORD_AUDIO` | Optional in-keyboard voice typing; recognition delegated to the system speech service. Not used to record/store/transmit audio. |
| `INTERNET` / `ACCESS_NETWORK_STATE` | Download user-selected input-method tables and check connectivity. |
| `POST_NOTIFICATIONS` | Status/progress notifications. |
| `VIBRATE` | Optional key-press haptics. |

---

## C. App content declarations (Section 10) — prepared answers

| Item | Answer |
| --- | --- |
| Privacy policy URL | `https://lime-ime.github.io/limeime/PRIVACY/` (publish [ANDROID_PRIVACY.md](ANDROID_PRIVACY.md) first) |
| Ads | **No ads** |
| App access | All functionality available without restrictions (no login) |
| Content rating | Complete IARC questionnaire → expected **Everyone** |
| Target audience | 18+ / general; **not** primarily for children |
| Government app | No |
| Financial features | No |
| Health | No |
| News app | No |
| Data safety | "No data collected/shared" (Section B) |

---

## D. Content rating (IARC) — prepared answers (Section 7)

Answer **No** to: violence, sexual content, profanity, controlled substances,
gambling, user-to-user communication, sharing user location, digital purchases.
Note `INTERNET` is used only to download input-method tables, not to browse
arbitrary web content. Expected result: **Everyone / PEGI 3**.

---

## E. Graphic asset spec sheet (Section 6)

Source logo in repo: [logo.png (xxhdpi 144×144)](../LimeStudio/app/src/main/res/drawable-xxhdpi/logo.png).
**The 144×144 source is too small for the 512 icon** — regenerate the icon from a
higher-resolution master (e.g. a vector/PNG ≥ 512px). If only the 144px raster
exists, recreate the mark at 512 rather than upscaling.

| Asset | Required | Spec | Source / how |
| --- | --- | --- | --- |
| **Hi-res app icon** | Yes | 512×512 PNG, 32-bit, alpha, ≤ 1 MB | Regenerate from a ≥512px master of the lime logo; do not upscale the 144px raster |
| **Feature graphic** | Yes | 1024×500 PNG/JPG, no alpha | Design banner: logo + "萊姆輸入法 / LimeIME" + tagline. No essential content near edges (Play overlays the title) |
| **Phone screenshots** | Yes (min 2) | 2–8 PNG/JPG, 16:9 or 9:16, min side 320px, max side 3840px | Capture on a real device: keyboard in a chat/notes app showing Zhuyin, Cangjie, English completion, emoji panel, voice key, settings |
| **7" tablet screenshots** | Recommended | up to 8, same format | Capture on 7" emulator/device in landscape |
| **10" tablet screenshots** | Recommended | up to 8, same format | Capture on 10" emulator/device in landscape |
| **Promo video** | Optional | YouTube URL | Skip for first release |

Screenshot capture tips:
- Use a clean device (status bar tidy, demo text in Traditional Chinese).
- Show the LimeIME keyboard, not just the settings screen — Play prefers
  in-context shots for keyboards.
- Avoid showing another app's full branded UI in a misleading way.

---

## F. Still requires a human decision / action (not draftable here)

- **Package identity — DECIDED: brand-new listing under `org.limeime`.**
  The old `net.toload.main.hd` listing (500k installs / 4.3★) **cannot be reused**
  — its Play developer account belongs to a now-inactive team member and is not
  under this team's control. Implications to accept:
  - This is a **fresh listing**: no inherited installs, reviews, or ratings; the
    rating/review count starts at zero.
  - Existing `net.toload.main.hd` users will **not** receive this as an update —
    it is a separate app they must find and install anew.
  - The Play listing id is **`org.limeime`** — **version- and ownership-neutral**.
    Chosen over the original `net.toload.*` prefix (named by the now-inactive
    original developer, meaning unknown) and over a year-stamped `hd2026`. ⚠️ This
    id is **permanent once published** — confirm it before the first upload, because
    it can never be changed afterward. *(Note: current beta builds still ship as
    `net.toload.main.hd2026`; `applicationId` is switched to `org.limeime` at Play
    onboarding — edit [build.gradle](../LimeStudio/app/build.gradle#L34) + the
    [test assertion](../LimeStudio/app/src/androidTest/java/org/limeime/ApplicationTest.java#L74) then.)*
  - **The Java package is now `org.limeime`**, unified with the `applicationId`
    (the namespace was previously `net.toload.main.hd`, kept separate from the app
    id). Classes live under `org.limeime`; the manifest's relative names
    (`.LIMEService`, `.LIMEBackupAgent`, `.ui.LIMESettings`) resolve against it.
    Because the IME service class moved, users updating from an older build must
    re-enable and re-select the keyboard once after this change.
  - In the store description, frame LIME IME 6 as the **continuation/successor** of
    the original LIME IME (the draft already does this) so returning users recognise
    it, even though the listing itself is new.
- **Developer/store display name**: cannot reuse the historic `Android.toload.net`
  account. Use this team's account; suggested display name
  **`The LimeIME Open Source Project`** (or your own).
- **Signing & AAB** (GOOGLE_PLAY.md Sections 2–3): create the upload keystore and
  wire a `signingConfig`; build a signed `.aab`. Left out per scope choice.
- **Distribution countries** (Section 12): choose target regions.
- **Publish the privacy policy** to GitHub Pages and confirm the live URL before
  entering it in Play Console.
- **Generate the 512 icon + feature graphic** from a hi-res master (design task).
