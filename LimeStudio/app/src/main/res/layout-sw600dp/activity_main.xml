<?xml version="1.0" encoding="utf-8"?>
<!--
  Tablet layout (sw600dp+): NavigationRail on start + FragmentContainerView filling the rest.
  Phone layout uses BottomNavigationView — see res/layout/.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    app:paddingTopSystemWindowInsets="true"
    tools:context=".ui.LIMESettings">

    <!-- Navigation Rail — tablet only (start side, vertical) -->
    <com.google.android.material.navigationrail.NavigationRailView
        android:id="@+id/main_nav_rail"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        app:paddingBottomSystemWindowInsets="true"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/main_nav" />

    <!-- Fragment container fills remaining width -->
    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1">

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/main_fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Activity-scoped progress overlay -->
        <FrameLayout
            android:id="@+id/activity_progress_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            android:background="#66000000">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp"
                android:layout_gravity="center">

                <TextView
                    android:id="@+id/activity_progress_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="10dp"
                    tools:text="Loading..."
                    android:textColor="#FFFFFF" />

                <ProgressBar
                    android:id="@+id/activity_progress_bar"
                    style="?android:attr/progressBarStyleHorizontal"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />

            </LinearLayout>
        </FrameLayout>

    </FrameLayout>

</LinearLayout>
